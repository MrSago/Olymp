
#include <bits/stdc++.h>

using namespace std;


int main() {
    ios_base::sync_with_stdio(false), cin.tie(0);
    int tt; cin >> tt;
    while (tt--) {
        int x, y; cin >> x >> y;
        cout << x - 1 << ' ' << y << '\n';
    }
    return 0;
}

